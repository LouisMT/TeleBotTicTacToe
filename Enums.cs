namespace TeleBotTicTacToe
{
    public enum State
    {
        None,
        Red,
        Blue
    }

    public enum Player
    {
        Red,
        Blue
    }

    public enum PlayerState
    {
        Win,
        Draw,
        Neutral
    }
}
